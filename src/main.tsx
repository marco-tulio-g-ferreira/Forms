import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './form1.tsx' //Obs: alterar entre formulários por aqui//
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
